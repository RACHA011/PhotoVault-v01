// material-ui
import { Typography } from '@mui/material';
import MainCard from 'components/MainCard';

const About = () => {
  return (
    <MainCard title="About">
      <Typography variant="body2">Its about rachadev and React</Typography>
    </MainCard>
  );
};

export default About;
